window.onload = function() {
	/*
	 
	alert("foobaa");
	
	var bt = tizen.bluetooth.getDefaultAdapter();
	
	// turn BT on
	bt.setPowered( true );
	bt.setVisible( true );
	
	alert( bt.address );
	
	var checkClose = function() {
		
		var cb = {};
		cb.onfinished = function( all ) {
			
			console.log( all.length );
			
		};
		
		bt.discoverDevices( cb );
		
	};
	
	*/
	
}